FROM python:3.9-slim
WORKDIR /app
COPY vr_controller.py .
# This Dockerfile is a placeholder.
# vr_controller.py only defines a class and does not run a service.
# A web framework like FastAPI or Flask is likely needed to expose the VRCampusEngine functionality.
